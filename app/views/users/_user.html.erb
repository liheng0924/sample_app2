<li>
  <%= gravatar_for user %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
  | <%= link_to 'Destroy', user, method: :delete, data: { confirm: ' Are you sure to delete '+user.name+' ?'} %>
  <%end%>
</li>
